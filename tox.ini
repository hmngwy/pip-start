[tox]
envlist = py36, base, test, develop

[testenv:production]
commands=pip install .

[testenv:test]
commands=python setup.py test

[testenv:develop]
commands=
    pip install -e '.[develop]'
    pytest --basetemp={envtmpdir}

[testenv]
basepython=python3.6
setenv =
    PYTHONPATH = {toxinidir}
